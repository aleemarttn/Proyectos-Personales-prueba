node_modules
dist
.DS_Store
*.local
.vite
.netlify

# Caché de la CLI de Supabase (se crea al desplegar Edge Functions)
supabase/.temp

# Variables de entorno (claves de Supabase, etc.) — NUNCA subir al repo
.env
.env.*
!.env.example
